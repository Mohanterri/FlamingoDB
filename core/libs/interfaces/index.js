const types = require('../types/index');

class collection {
    constructor(){
        
    }

    getName () {this.name;}

    setName (name) {this.name = name;}
}

module.exports = {
    collection
}